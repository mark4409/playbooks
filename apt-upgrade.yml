---
 - name: Apt Upgrade
   hosts: laptops
   tasks:
    - name: Update cache 
      become: yes
      apt:
        update_cache: yes
         
    - name: Upgrade all packages on servers
      become: yes
      apt: 
        name: "*"
        state: latest
